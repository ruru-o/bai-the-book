package com.baithebook

import android.app.Application

class App : Application() {
    override fun onCreate() {
        super.onCreate()
        // Initialize global app configuration if needed
    }
}
